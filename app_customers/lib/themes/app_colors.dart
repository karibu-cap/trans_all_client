import 'package:flutter/material.dart';

/// List of the Customers app colors.
class AppColors {
  /// Transparent Color.
  static const Color transparent = Colors.transparent;

  /// Green Color.
  static const Color green = Color.fromARGB(255, 28, 76, 46);

  /// Green Color.
  static const Color purple = Color.fromRGBO(155, 102, 238, 1);

  /// Black Color.
  static const Color black = Color.fromRGBO(103, 101, 101, 1);

  /// Black Color.
  static const Color lightBlack = Color.fromARGB(255, 114, 114, 114);

  /// Black Color.
  static const Color darkBlack = Color.fromARGB(255, 17, 17, 17);

  /// Teal Color.
  static const Color teal = Color(0xff00d4a2);

  /// Teal Color.
  static const Color lightGreen = Color.fromARGB(255, 38, 189, 93);

  /// Turquois Color.
  static const Color turquois = Color(0xff7bebff);

  /// Blue Color.
  static const Color blue = Color.fromRGBO(25, 68, 214, 1);

  /// Dark Blue Color.
  static const Color darkBlue = Color(0xff0e5c7c);

  /// Red Color.
  static const Color red = Color.fromARGB(255, 226, 112, 70);

  /// Red Color.
  static const Color red2 = Color.fromARGB(255, 231, 58, 58);

  /// Orange Color.
  static const Color orange = Color.fromRGBO(235, 113, 46, 1);

  /// Gray Color.
  static const Color gray = Color(0xffb2b2b2);

  /// Gray Color.
  static const Color lightGray = Color.fromRGBO(0, 0, 0, 0.231);

  /// Dark Gray Color.
  static const Color darkGray = Color(0xff202020);

  /// White Color.
  static const Color white = Color(0xffffffff);
}
